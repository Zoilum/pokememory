import React from "react";
import { render, screen } from "@testing-library/react";
import App from "./App";

// TODO

test("creates 8 folded cards starting by 4 pokemons", () => {});

test("turnes clicked card", () => {});

test("turnes max 2 cards", () => {});

test("checks if 2 cards match", () => {
  test("folds cards if they don't match", () => {});

  test("doesn't fold cards if they match", () => {});
});

test("checks if all cards are matched", () => {
  test("shows 'YOU WIN' component if all cards match", () => {});
});
